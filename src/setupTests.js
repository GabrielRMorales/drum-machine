// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
import { getByTestId } from '@testing-library/react';

/*describe("has an outer container with a corresponding id of drum-machine' that containers other elements",()=>{
    it("should have an outer container with a corresponding id of drum-machine",()=>{
        let outerContainer = document.body.children;
        expect(outerContainer.length).toBe(1);

    });
})*/